<UserControl x:Class="WPF_MVVM_SimplePageNavigation.Pages.PageC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MVVM_SimplePageNavigation.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Orange">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />

        </Grid.RowDefinitions>
        <TextBlock Text="Page C" FontSize="26"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <TextBlock Text="{Binding Test, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}" FontSize="26"
                   HorizontalAlignment="Center"
                   VerticalAlignment="top"
                   Grid.Row="1"/>

    </Grid>
</UserControl>
